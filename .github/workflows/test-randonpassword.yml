name: Test randompassword
on:
  push:
  schedule:
    - cron: '23 6 * * *'
jobs:
  test-randompassword:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: bash test_randompassword
