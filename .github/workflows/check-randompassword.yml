name: Test randompassword
run-name: ${{ github.actor }}
on: [push]
jobs:
  check-randompassword:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: sudo apt install shellcheck
      - run: bash test_randompassword
